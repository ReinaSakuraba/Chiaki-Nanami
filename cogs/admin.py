import discord
import inspect
import traceback

from discord.ext import commands
from .utils import checks
from .utils.misc import code_msg

class Admin:
    # Will add admin-only commands (if I can think of any)
    pass
        
def setup(bot):
    pass
